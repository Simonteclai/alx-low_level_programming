#include "main.h"

/**
 * _strcat - comcaterates two strings
 * @dest: string one
 * @src: string two
 * Return: *_strcat
 */

char *_strcat(char *dest, char *src);
{
	*_strcat = strcat(*dest, *src);
	return (*_strcat);
}
