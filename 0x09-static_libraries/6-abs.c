#include "main.h"
#include <stdlib.h>
#include <math.h>
/**
 * _abs - computes the absoulut
 * @b: integer to absoulute value
 * Return: the integer
 */
int _abs(int b)
{
	if (b >= 0)
		return (b);
	else
		return (-b);
}
