#include <stdio.h>
#include <string.h>
/**
 * 
 * main - Entry piont
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ermi[70];
	strcpy(ermi,"\"Programming is like building a multilingual puzzle");
	puts(ermi);
        return(0);
}
