#include <stdio.h>

/**
  * main - Entry point
  * Discription: prints the given line exactly as it is
  * Return: 0 (Success)
  */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
