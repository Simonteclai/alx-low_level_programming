#include "main"
/**
 * int _islower - checks if the alphabets are lower or not
 *
 * Return: 0 (Success)
 */
int _islower(int c);
{
	char ch;

	if ( islower(ch) )
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
