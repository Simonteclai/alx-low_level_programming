#include "main.h"
#include <math.h>
/**
 * add - adds the given numbers
 * @a: first oprand
 * @b: second oprand to be added
 * Return: the result
 */
int add(int a, int b)
{
	int result = a + b;

	_putchar(result + '0');
	return (result);
}
